Create or activate a reverse case for the supplied APK. Read the persisted analysis request, decompose it into concrete questions and success criteria, choose only the necessary JADX, IDA, Frida, Unidbg, replay, and verification routes, then write/register `artifacts/analysis-plan.md` and publish the dynamic pipeline with `reverse_analysis_plan` before invoking target-analysis tools. Advance it with `reverse_analysis_step`. Preserve evidence and omit unnecessary routes. If REPORT is selected, write/register `artifacts/report.md` with goal-specific findings and mark the report stage completed. Do not stop at a generic tool list.
